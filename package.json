{
  "name": "second-brain-project",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "install:all": "yarn install",
    "dev": "concurrently \"yarn workspace sbp-frontend dev\" \"yarn workspace sbp-backend start:dev\"",
    "build": "yarn workspace sbp-frontend build",
    "start": "yarn workspace sbp-frontend start"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
